diff --git a/module/evdi_drm_drv.c b/module/evdi_drm_drv.c
index 09a9da0..06a799d 100644
--- a/module/evdi_drm_drv.c
+++ b/module/evdi_drm_drv.c
@@ -124,9 +124,14 @@ static struct drm_driver driver = {
 
 	.fops = &evdi_driver_fops,
 
+// Went all internal with https://github.com/torvalds/linux/commit/71a7974ac7019afeec105a54447ae1dc7216cbb3 and is thus not required anymore
+#if KERNEL_VERSION(6, 6, 0) > LINUX_VERSION_CODE
 	.prime_fd_to_handle = drm_gem_prime_fd_to_handle,
 	.gem_prime_import = drm_gem_prime_import,
 	.prime_handle_to_fd = drm_gem_prime_handle_to_fd,
+#else
+	.gem_prime_import = drm_gem_prime_import,
+#endif
 #if KERNEL_VERSION(5, 11, 0) <= LINUX_VERSION_CODE || defined(EL8)
 #else
 	.preclose = evdi_driver_preclose,
diff --git a/module/evdi_fb.c b/module/evdi_fb.c
index 3789b33..f880dcb 100644
--- a/module/evdi_fb.c
+++ b/module/evdi_fb.c
@@ -418,7 +418,10 @@ static int evdifb_create(struct drm_fb_helper *helper,
 	info->fix.smem_start = (unsigned long)efbdev->efb.obj->vmapping;
 
 #if KERNEL_VERSION(4, 20, 0) <= LINUX_VERSION_CODE || defined(EL8)
+  // FBINFO_DEFAULT was removed in Kernel 6.6.0
+  #ifdef FBINFO_DEFAULT
 	info->flags = FBINFO_DEFAULT;
+  #endif
 #else
 	info->flags = FBINFO_DEFAULT | FBINFO_CAN_FORCE_OUTPUT;
 #endif
