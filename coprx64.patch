diff --git a/.github/workflows/buildcheck.yml b/.github/workflows/buildcheck.yml
deleted file mode 100644
index 16061d1..0000000
--- a/.github/workflows/buildcheck.yml
+++ /dev/null
@@ -1,106 +0,0 @@
-name: Build Check
-
-on:
-  push:
-  pull_request:
-    branches: [ master ]
-
-jobs:
-  makeTest:
-    runs-on: ubuntu-latest
-    container: 
-      image: quay.io/rockylinux/rockylinux:8
-    steps:
-      - uses: actions/checkout@v3
-
-      - name: Test Makefile Targets
-        run: ci/testbuilding.sh
-
-  buildTest:
-    needs: makeTest
-    runs-on: ubuntu-latest
-    container: 
-      image: ${{ matrix.dockernamespace }}:${{ matrix.version }}
-    strategy:
-      matrix:
-        include:
-          - os: fedora
-            version: 30
-            dockernamespace: fedora
-          - os: fedora
-            version: 31
-            dockernamespace: fedora
-          - os: fedora
-            version: 32
-            dockernamespace: fedora
-          - os: fedora
-            version: 33
-            dockernamespace: fedora
-          - os: fedora
-            version: 34
-            dockernamespace: fedora
-          - os: fedora
-            version: 35
-            dockernamespace: fedora
-          - os: fedora
-            version: 36
-            dockernamespace: quay.io/fedora/fedora
-          - os: fedora
-            version: 37
-            dockernamespace: quay.io/fedora/fedora
-          - os: fedora
-            version: 38
-            dockernamespace: quay.io/fedora/fedora
-          - os: centos
-            version: 7.9.2009
-            dockernamespace: quay.io/centos/centos
-          - os: centos-stream
-            version: stream8
-            dockernamespace: quay.io/centos/centos
-          - os: centos-stream
-            version: stream9
-            dockernamespace: quay.io/centos/centos
-          - os: rocky
-            version: 8
-            dockernamespace: quay.io/rockylinux/rockylinux
-          - os: rocky
-            version: 9
-            dockernamespace: quay.io/rockylinux/rockylinux
-
-    steps:
-      - uses: actions/checkout@v3
-
-      - name: Specify Build Target
-        run: |
-          echo "SPECIFICTARGET=devel" >> $GITHUB_ENV
-          
-      - name: Environment variable for OS version
-        run: |
-          echo "OSVERSION=${{ matrix.version }}" >> $GITHUB_ENV
-
-      - if: always() && matrix.version < 37 || startsWith(matrix.os, 'centos')
-        name: Run CI
-        run: ci/${{ matrix.os }}.sh
-
-      - if: always() && matrix.os == 'fedora' && matrix.version > 36
-        name: Run CI (Fedora 37+)
-        run: ci/${{ matrix.os }}37plus.sh
-
-  mockTest:
-    needs: makeTest
-    runs-on: ubuntu-latest
-    container: 
-      image: quay.io/rockylinux/rockylinux:8
-      options: --privileged
-    steps:
-      - uses: actions/checkout@v3
-
-      - name: Test Mock Build From Spec
-        run: |
-          echo "Install Mock"
-          dnf -y install epel-release
-          dnf -y install mock gcc gcc-c++ glibc-devel.x86_64 glibc-devel.i686 libdrm-devel rpm-build make wget
-          echo "Get source files"
-          make
-          echo "Run mock build"
-          mock -r rocky+epel-8-x86_64 --spec displaylink.spec --source .
diff --git a/.github/workflows/devbuild.yml b/.github/workflows/devbuild.yml
deleted file mode 100644
index c737b44..0000000
--- a/.github/workflows/devbuild.yml
+++ /dev/null
@@ -1,75 +0,0 @@
-name: Devel Branch Build
-
-on:
-  schedule:
-    - cron: '30 5 * * 1'
-
-jobs:
-  develBuild:
-    runs-on: ubuntu-latest
-    container: 
-      image: ${{ matrix.dockernamespace }}:${{ matrix.version }}
-    strategy:
-      matrix:
-        include:
-          - os: fedora
-            version: 30
-            dockernamespace: fedora
-          - os: fedora
-            version: 31
-            dockernamespace: fedora
-          - os: fedora
-            version: 32
-            dockernamespace: fedora
-          - os: fedora
-            version: 33
-            dockernamespace: fedora
-          - os: fedora
-            version: 34
-            dockernamespace: fedora
-          - os: fedora
-            version: 35
-            dockernamespace: fedora
-          - os: fedora
-            version: 36
-            dockernamespace: quay.io/fedora/fedora
-          - os: fedora
-            version: 37
-            dockernamespace: quay.io/fedora/fedora
-          - os: fedora
-            version: 38
-            dockernamespace: quay.io/fedora/fedora
-          - os: centos
-            version: 7.9.2009
-            dockernamespace: quay.io/centos/centos
-          - os: centos-stream
-            version: stream8
-            dockernamespace: quay.io/centos/centos
-          - os: centos-stream
-            version: stream9
-            dockernamespace: quay.io/centos/centos
-          - os: rocky
-            version: 8
-            dockernamespace: quay.io/rockylinux/rockylinux
-          - os: rocky
-            version: 9
-            dockernamespace: quay.io/rockylinux/rockylinux
-
-    steps:
-      - uses: actions/checkout@v3
-
-      - name: Specify Build Target
-        run: |
-          echo "SPECIFICTARGET=devel" >> $GITHUB_ENV
-
-      - name: Environment variable for OS version
-        run: |
-          echo "OSVERSION=${{ matrix.version }}" >> $GITHUB_ENV
-
-      - name: Run CI
-        if: matrix.version < 37 || startsWith(matrix.os, 'centos')
-        run: ci/${{ matrix.os }}.sh
-      
-      - name: Run CI (Fedora 37+)
-        if: matrix.os == 'fedora' && matrix.version > 36
-        run: ci/${{ matrix.os }}37plus.sh
diff --git a/.github/workflows/rawhidebuilds.yml b/.github/workflows/rawhidebuilds.yml
deleted file mode 100644
index a51b93d..0000000
--- a/.github/workflows/rawhidebuilds.yml
+++ /dev/null
@@ -1,27 +0,0 @@
-name: Rawhide Build
-
-on:
-  schedule:
-    - cron: '45 5 * * 1'
-
-jobs:
-  rawhideBuilds:
-    runs-on: ubuntu-latest
-    container: 
-      image: ${{ matrix.dockernamespace }}:${{ matrix.version }}
-    strategy:
-      matrix:
-        include:
-          - os: fedora
-            version: rawhide
-            dockernamespace: quay.io/fedora/fedora
-
-    steps:
-      - uses: actions/checkout@v3
-
-      - name: Specify Build Target
-        run: |
-          echo "SPECIFICTARGET=rawhide" >> $GITHUB_ENV
-
-      - name: Run CI
-        run: ci/${{ matrix.os }}37plus.sh
diff --git a/.github/workflows/release.yml b/.github/workflows/release.yml
index 72b52ab..324706f 100644
--- a/.github/workflows/release.yml
+++ b/.github/workflows/release.yml
@@ -13,48 +13,15 @@ jobs:
     strategy:
       matrix:
         include:
-        - os: fedora
-          version: 30
-          dockernamespace: fedora
-        - os: fedora
-          version: 31
-          dockernamespace: fedora
-        - os: fedora
-          version: 32
-          dockernamespace: fedora
-        - os: fedora
-          version: 33
-          dockernamespace: fedora
-        - os: fedora
-          version: 34
-          dockernamespace: fedora
-        - os: fedora
-          version: 35
-          dockernamespace: fedora
-        - os: fedora
-          version: 36
-          dockernamespace: quay.io/fedora/fedora
         - os: fedora
           version: 37
           dockernamespace: quay.io/fedora/fedora
         - os: fedora
           version: 38
           dockernamespace: quay.io/fedora/fedora
-        - os: centos
-          version: 7.9.2009
-          dockernamespace: quay.io/centos/centos
-        - os: centos-stream
-          version: stream8
-          dockernamespace: quay.io/centos/centos
-        - os: centos-stream
-          version: stream9
-          dockernamespace: quay.io/centos/centos
-        - os: rocky
-          version: 8
-          dockernamespace: quay.io/rockylinux/rockylinux
-        - os: rocky
-          version: 9
-          dockernamespace: quay.io/rockylinux/rockylinux
+        - os: fedora
+          version: 39
+          dockernamespace: quay.io/fedora/fedora
 
     steps:
       - uses: actions/checkout@v3
@@ -78,13 +45,8 @@ jobs:
       - name: Environment variable for OS version
         run: |
           echo "OSVERSION=${{ matrix.version }}" >> $GITHUB_ENV
-     
-      - name: Run CI
-        if: matrix.version < 37 || startsWith(matrix.os, 'centos')
-        run: ci/${{ matrix.os }}.sh
 
-      - name: Run CI (Fedora 37+)
-        if: matrix.os == 'fedora' && matrix.version > 36
+      - name: Run CI
         run: ci/${{ matrix.os }}37plus.sh
 
       - name: Run build
@@ -94,7 +56,6 @@ jobs:
         run : ls -la
       - run: mv displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.src.rpm ${{ matrix.os }}-${{ matrix.version }}-displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.src.rpm
       - run: mv x86_64/displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.x86_64.rpm ${{ matrix.os }}-${{ matrix.version }}-displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.x86_64.rpm
-      - run: mv i386/displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.i386.rpm ${{ matrix.os }}-${{ matrix.version }}-displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.i386.rpm
       - run: chown `id -u`:`id -g` -R .
 
       - name: Upload RPM
@@ -113,3 +74,16 @@ jobs:
           update_latest_release: true
           tags: true
           overwrite: true
+
+      - name: Install copr-cli
+        if: matrix.version > 38
+        run: dnf install copr-cli -y
+
+      - name: Add login to copr
+        if: matrix.version > 38
+        run: |
+          echo "${{ secrets.COPR_CREDENTIALS }}" >> "./coprConfig"
+      
+      - name: Upload to COPR
+        if: matrix.version > 38
+        run: copr-cli --config "./coprConfig" build Displaylink ${{ matrix.os }}-${{ matrix.version }}-displaylink-${{ env.VERSION }}-${{ env.RELEASE }}${{ env.EVDISOURCE }}.src.rpm
diff --git a/Makefile b/Makefile
index 96c347e..a8f119f 100644
--- a/Makefile
+++ b/Makefile
@@ -33,15 +33,14 @@ i386_RPM   := i386/displaylink-$(VERSION)-$(RELEASE).i386.rpm
 x86_64_RPM := x86_64/displaylink-$(VERSION)-$(RELEASE).x86_64.rpm
 SRPM       := displaylink-$(VERSION)-$(RELEASE).src.rpm
 
-TARGETS    := $(i386_RPM) $(x86_64_RPM) $(SRPM)
+TARGETS    := $(x86_64_RPM) $(SRPM)
 
 # Use release found on GitHub instead of what comes in the
 # Displaylink download
-i386_RPM_GITHUB_EVDI   := i386/displaylink-$(VERSION)-$(RELEASE)-github_evdi.i386.rpm
 x86_64_RPM_GITHUB_EVDI := x86_64/displaylink-$(VERSION)-$(RELEASE)-github_evdi.x86_64.rpm
 SRPM_GITHUB_EVDI       := displaylink-$(VERSION)-$(RELEASE)-github_evdi.src.rpm
 
-TARGETS_GITHUB_EVDI := $(i386_RPM_GITHUB_EVDI) $(x86_64_RPM_GITHUB_EVDI) $(SRPM_GITHUB_EVDI)
+TARGETS_GITHUB_EVDI := $(x86_64_RPM_GITHUB_EVDI) $(SRPM_GITHUB_EVDI)
 
 #
 # Upstream checks
@@ -77,11 +76,11 @@ all: $(TARGETS)
 # Use evdi tagged release on Github instead of using what is bundled in Displaylink download
 github-release: $(TARGETS_GITHUB_EVDI)
 
-rpm: $(i386_RPM) $(x86_64_RPM)
+rpm: $(x86_64_RPM)
 
-srpm: $(i386_RPM) $(x86_64_RPM)
+srpm: $(x86_64_RPM)
 
-rpm-github: $(i386_RPM_GITHUB_EVDI) $(x86_64_RPM_GITHUB_EVDI)
+rpm-github: $(x86_64_RPM_GITHUB_EVDI)
 
 srpm-github: $(SRPM_GITHUB_EVDI)
 
@@ -105,7 +104,7 @@ clean-mainline:
 clean: clean-mainline clean-rawhide
 
 clean-all:
-	rm -rf i386/*.rpm x86_64/*.rpm displaylink*.src.rpm $(EVDI_PKG) $(EVDI_DEVEL)
+	rm -rf x86_64/*.rpm displaylink*.src.rpm $(EVDI_PKG) $(EVDI_DEVEL)
 
 # for testing our version construction
 versions:
@@ -148,22 +147,12 @@ BUILD_DEFINES =                                                     \
 
 BUILD_DEFINES_GITHUB_EVDI = --define "_github 1"
 
-$(i386_RPM): $(BUILD_DEPS)
-	CFLAGS='-m32 -march=i386' \
-	LDFLAGS='-m32 -march=i386' \
-	rpmbuild -bb $(BUILD_DEFINES) displaylink.spec --target=i386
-
 $(x86_64_RPM): $(BUILD_DEPS)
 	rpmbuild -bb $(BUILD_DEFINES) displaylink.spec --target=x86_64
 
 $(SRPM): $(BUILD_DEPS)
 	rpmbuild -bs $(BUILD_DEFINES) displaylink.spec
 
-$(i386_RPM_GITHUB_EVDI): $(BUILD_DEPS_GITHUB_EVDI)
-	CFLAGS='-m32 -march=i386' \
-	LDFLAGS='-m32 -march=i386' \
-	rpmbuild -bb $(BUILD_DEFINES)$(BUILD_DEFINES_GITHUB_EVDI) displaylink.spec --target=i386
-
 $(x86_64_RPM_GITHUB_EVDI): $(BUILD_DEPS_GITHUB_EVDI)
 	rpmbuild -bb $(BUILD_DEFINES)$(BUILD_DEFINES_GITHUB_EVDI) displaylink.spec --target=x86_64
 
